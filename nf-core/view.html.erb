<%# Offers a quick link to the user's working directory via the OnDemand interface.
This will appear once the job begins running. %>
<p>The pipeline does not offer an interactive interface. <a href= "/pun/sys/files/fs/<%= workdir %>/<%= pipeline %>/<%= ENV['SLURM_JOB_ID'] %>">You can find your output files here.</a></p>

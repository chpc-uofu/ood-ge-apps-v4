profiles {
    chpc {
        process {
            executor = 'slurm'
            queue = '<%= context.cpu_partition %>'
            clusterOptions = '<%= context.account %>'
            withLabel: alignment {
                cpus = 16
                memory = 30.GB
            }

    }
}